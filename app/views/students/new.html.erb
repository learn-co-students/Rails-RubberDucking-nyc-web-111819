<h1>New Student Form</h1>
<br>
<% if flash[:messages]%>
  <h2><%= flash[:messages].join(", ") + "." %></h2>
<% end%>

<%= form_for(@student) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :mod %>
  <%= f.number_field :mod %>
  <%= f.submit %>
<% end %>