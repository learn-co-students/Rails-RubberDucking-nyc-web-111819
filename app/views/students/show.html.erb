<h1>Student Show Page</h1>

<h3>Student:</h3> 
<%= @student.name %>, mod <%= @student.mod %>
<br><br>
Ducks: 
<ul>
<% @student.ducks.each do |duck| %>
    <li><%= duck.name %></li>
    <% end %>
</ul>

<%= button_to "DELETE", student_path(@student), method: :delete %>